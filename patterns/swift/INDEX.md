# Swift Patterns — Index

Read this file whole; open an entry only when it applies. Format per `patterns/README.md`.

| Pattern | Problem it solves |
|---|---|
| `liquid-glass-chrome` | Adopting Liquid Glass: what gets glass for free, when custom chrome earns `glassEffect`, what never does |
| `sheet-with-detents` | Presenting a task over a place: detent choice, dismissal rules, background interaction |
| `navigation-stack-path` | Navigation as state: one path owner, deep links as state restoration |
| `observable-model` | State wiring: `@Observable` model, `@State` ownership, environment injection |
| `list-row-actions` | Row actions: swipe + context menu + rotor custom actions, destructive placement |
| `dynamic-type-layout` | Layouts that survive AX sizes: axis-switching, scaled metrics, no clipped containers |
| `commit-feedback` | The commit micro-interaction: spring + haptic pairing + reduce-motion variant |
| `empty-state` | Zero-state that teaches the first action, on the system component |

Uncovered problem? Solve it, then flag it as a harvest candidate (see Lifecycle in `patterns/README.md`).
